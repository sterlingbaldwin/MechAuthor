# Planner Workspace

Load prompt files from `prompts/`.
Use `prompts/main_system_prompt.md` as the system prompt.
Set `STATE_ROOT=../../global_state` when using this package in-place.
If shared state is mounted elsewhere in OpenClaw, set `STATE_ROOT` to that mounted path.
